apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Addon library: https://github.com/rayabueg/platform-addons
# The ?ref= below is the uprev surface — bump it to a new platform-vX.Y.Z tag
# to roll this addon forward across every cluster that subscribes to base/external-secrets.
resources:
  - https://github.com/rayabueg/platform-addons.git//external-secrets/manifests?ref=platform-v0.1.0
